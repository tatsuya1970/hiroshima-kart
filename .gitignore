node_modules
dist
data/citygml
*.log
data/shots*
data/dem_probe.txt
data/lod2tex

# プロモ動画の素材 (videos/)。連番 PNG と書き出しは重いので入れない。
# 作り直しは: node tools/record_promo.mjs → node tools/clips_to_mp4.mjs videos/assets
videos/
# TURN の設定 (npm run turn:prepare が書き出す。資格情報 API の URL を含むことがある)
public/turn.json
# wrangler の作業ファイル (wrangler dev のキャッシュ・ローカル状態)
.wrangler/
